{% set classes = [
  'chip',
  'chip--color-' ~ color,
  modifier ? modifier : '',
]|join(' ')|trim %}

<div {{ add_attributes({ class: classes }) }}>
  {# {% include '@components/button/button.twig' %} #}

  {% block chip_content %}
    {{ content }}
  {% endblock %}
</div>
